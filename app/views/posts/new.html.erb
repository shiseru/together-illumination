<div class="main">
  <div class="container">
    <h1>New Post</h1>

    <%= render 'form', post: @post %>

    <%= link_to 'Back', posts_path %>

    <p>Location：<br>
      <select name="Location">
        <% @illuminations.each do |illumination|%>
          <option value="#{illumination}"><%= illumination.name%></option>
          <%#= form :location %>
        <%end%>
      </select>
    </p>
  </div>
</div>

